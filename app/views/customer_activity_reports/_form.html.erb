<%= form_for(@customer_activity_report) do |f| %>
  <% if @customer_activity_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_activity_report.errors.count, "error") %> prohibited this customer_activity_report from being saved:</h2>

      <ul>
      <% @customer_activity_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
